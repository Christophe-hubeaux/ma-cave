# Environnement virtuel Python
venv/
__pycache__/
*.pyc

# Fichier de secrets (clé API)
.env

# Base de données locale (ne pas versionner ; en prod on utilisera
# soit un disque persistant, soit une base PostgreSQL)
cave.db